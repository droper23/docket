{
  "name": "learningsuite-reskin",
  "version": "0.1.0",
  "private": true,
  "description": "Safari userscript: an Apple-HIG-styled visual/interaction layer over BYU LearningSuite. LearningSuite stays the real backend.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "pretest": "node test-build.mjs",
    "test": "node --test dist-test/*.test.js"
  },
  "devDependencies": {
    "@types/jsdom": "^30.0.0",
    "esbuild": "^0.24.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3"
  }
}
